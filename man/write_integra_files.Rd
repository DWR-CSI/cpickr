% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_integra_files.R
\name{write_integra_files}
\alias{write_integra_files}
\title{Write a tibble to multiple semi-colon delimited files for Integra Assist Plus robot input}
\usage{
write_integra_files(
  data,
  target_plate_prefix = "TARGET",
  file_prefix = "integra_input_",
  output_dir = ".",
  max_source_plates = 3,
  transfer_volume = 25,
  auto_dest_well = TRUE
)
}
\arguments{
\item{data}{A tibble or data.frame to be split and written. Must contain columns
'SampleID', 'PlateID', and 'WellID'.}

\item{target_plate_prefix}{Prefix for the destination plate IDs. Default is "TARGET".}

\item{file_prefix}{Prefix for the output filenames. Default is "integra_input_".}

\item{output_dir}{Directory where the output files should be written. Default is "." (current working directory).}

\item{max_source_plates}{Maximum number of unique source plates allowed per run file. Default is 3.}

\item{transfer_volume}{Default volume (in microliters) to transfer if a 'TransferVolume'
column is not present in 'data'. Default is 25.}

\item{auto_dest_well}{If TRUE, the target well will be automatically filled in
sequentially by column (A1, B1, C1, etc.). Default is TRUE. If FALSE, an empty
target well column will be created.}
}
\value{
Nothing is returned, but files are written to the target directory:
\itemize{
\item Individual input files with format \code{[file_prefix][index].csv} (semi-colon delimited)
\item A summary file \code{[file_prefix]summary.tsv} (tab-separated values) listing the rounds
}
}
\description{
This function splits a tibble into multiple semi-colon delimited files based on specified criteria,
formatting the output for use with Integra Assist Plus robots in cherrypicking operations.
The function creates formatted files with required columns (SampleID, SourcePlateID, SourceWell,
TargetPlateID, TargetWell, TransferVolume) and also generates a summary file (TSV) mapping the rounds
on the robot to their source and destination plates.
}
\examples{
\dontrun{
data <- tibble::tibble(
  SampleID = c("S1", "S2", "S3", "S4"),
  PlateID = c("PlateA", "PlateA", "PlateB", "PlateC"),
  WellID = c("A1", "A2", "B1", "C5")
)
write_integra_files(data)
}
}
